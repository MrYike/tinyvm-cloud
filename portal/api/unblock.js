import {clearAccess} from './_lib/store.js';import {safeEqual} from './_lib/security.js';
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.ADMIN_SECRET||!safeEqual(req.headers.authorization,`Bearer ${process.env.ADMIN_SECRET}`))return res.status(401).json({error:'Unauthorized'});const ip=String(req.body?.ip||'').trim();if(!ip)return res.status(400).json({error:'IP is required'});try{await clearAccess(ip);return res.status(200).json({ok:true,ip})}catch(e){return res.status(503).json({error:e.message})}}
