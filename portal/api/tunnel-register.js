import {safeEqual} from './_lib/security.js';
import {saveTunnel} from './_lib/store.js';
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const supplied=String(req.headers.authorization||'').replace(/^Bearer\s+/i,'');if(!process.env.FILE_BRIDGE_TOKEN||!safeEqual(supplied,process.env.FILE_BRIDGE_TOKEN))return res.status(401).json({error:'Unauthorized'});try{const url=new URL(String(req.body?.url||''));if(url.protocol!=='https:'||!url.hostname.endsWith('.trycloudflare.com'))return res.status(400).json({error:'Invalid tunnel URL'});await saveTunnel({url:url.origin,time:new Date().toISOString()});return res.status(200).json({ok:true})}catch(error){return res.status(400).json({error:error.message})}}
